query marketTokensSearch($text: String!, $first: Int!, $skip: Int!) {
    marketSearch(text: $text, first: $first, skip: $skip) {
        tokenId
        contractId
        ownerId
        title
        description
        media
        mintSiteName,
        mintSiteLink,
        price
    }
}