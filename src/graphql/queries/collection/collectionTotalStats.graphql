query collectionTotalStats(
    $contractId: ID!,
    $_contractId: String!
) {
    stats: totalCollectionStat(
        id: $contractId
    ) {
        volume
        sales,
        avg,
    },
    floar: marketTokens(
        where: {
            contractId: $_contractId
        },
        orderBy: price,
        orderDirection: asc,
        first: 1
    ) {
        id
        price
    }
}