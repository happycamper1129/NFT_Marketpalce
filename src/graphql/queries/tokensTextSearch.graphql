query marketTokensSearch($text: String!) {
    marketSearch(text: $text) {
        ownerId
        tokenId
        contractId
        title
        description
        media
        copies
        ipfsReference
        price
    }
}