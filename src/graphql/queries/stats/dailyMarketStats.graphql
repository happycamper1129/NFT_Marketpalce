query dailyMarketStats(
    $fromTimestamp: BigInt
) {
    dailyMarketStats(
        where: {
            timestamp_gte: $fromTimestamp
        },
        orderBy: timestamp
    ) {
        volume,
        sales
        timestamp,
    }
}