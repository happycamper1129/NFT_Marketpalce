query collectionsTextSearch(
    $text: String!
    $limit: Int!
    $offset: Int!
) @api(name: collections) {
    collections: collectionsSearch(
        text: $text,
        first: $limit
        skip: $offset
    ) {
        id
        ownerId
        collectionId
        title
        description
        image
    }
}