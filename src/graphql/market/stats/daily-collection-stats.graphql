query dailyCollectionStats(
    $collection: String!
) @api(name: market) {
    dailyCollectionStats(
        where: {
            collection: $collection
        }
    ) {
        id
        timestamp
        sales
        average
        floor
        volume
    }
}