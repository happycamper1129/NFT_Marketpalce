query collections(
    $limit: Int!,
    $offset: Int!
) @api(name: collections) {
    collections(
        first: $limit
        skip: $offset
        orderBy: createdAt
        orderDirection: desc
    ) {
        id
        ownerId
        collectionId
        title
        description
        image
    }
}