query marketStatistics(
    $fromTimestamp: BigInt!
) @api(name: market) {
    dailyMarketStats(
        where: {
            timestamp_gte: $fromTimestamp
        },
        orderBy: timestamp
    ) {
        id
        volume
        sales
        timestamp
    }
}