export interface MarketNft {
    approval_id: number
    nft_contract_id: string
    owner_id: string
    price: number
    token_id: string
}

export interface PayableToken {
    tokenUID: string,
    price: string
}