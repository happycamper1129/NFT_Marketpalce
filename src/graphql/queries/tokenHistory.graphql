query tokenActivity(
    $tokenUID: String!
) {
    activities(
        where: {
            token: $tokenUID
        },
        orderBy: timestamp,
        orderDirection: desc
    ) {
        ownerId,
        buyerId,
        price
        txHash,
        eventType,
        timestamp
    }
}